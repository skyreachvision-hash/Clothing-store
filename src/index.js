import { getEmailSettings } from "./email-service.js";

const JSON_HEADERS = {
  "Content-Type": "application/json; charset=utf-8",
  "Cache-Control": "no-store"
};
const FIREBASE_PROJECT_ID = "clothing-store-e7200";
const FIREBASE_ISSUER = `https://securetoken.google.com/${FIREBASE_PROJECT_ID}`;
const FIREBASE_KEYS_URL = "https://www.googleapis.com/robot/v1/metadata/x509/securetoken@system.gserviceaccount.com";
const firebaseKeyCache = new Map();
let firebaseKeysExpiresAt = 0;
function jsonResponse(payload, status = 200) { return new Response(JSON.stringify(payload), { status, headers: JSON_HEADERS }); }
function base64UrlToBytes(value) { const padded = value.replace(/-/g, "+").replace(/_/g, "/") + "==="; const binary = atob(padded.slice(0, Math.floor(padded.length / 4) * 4)); const bytes = new Uint8Array(binary.length); for (let index = 0; index < binary.length; index += 1) bytes[index] = binary.charCodeAt(index); return bytes; }
function decodeBase64UrlJson(value) { return JSON.parse(new TextDecoder().decode(base64UrlToBytes(value))); }
async function getFirebaseKeys() {
  if (Date.now() < firebaseKeysExpiresAt && firebaseKeyCache.size) return firebaseKeyCache;
  const response = await fetch(FIREBASE_KEYS_URL, { headers: { Accept: "application/json" }, cf: { cacheTtl: 300 } });
  if (!response.ok) throw new Error("Unable to load Firebase public keys.");
  const cacheControl = response.headers.get("Cache-Control") || "";
  const maxAgeMatch = cacheControl.match(/max-age=(\d+)/i);
  const maxAge = maxAgeMatch ? Number(maxAgeMatch[1]) : 3600;
  const keys = await response.json();
  firebaseKeyCache.clear();
  for (const [kid, certificate] of Object.entries(keys)) firebaseKeyCache.set(kid, certificate);
  firebaseKeysExpiresAt = Date.now() + Math.max(60, maxAge) * 1000;
  return firebaseKeyCache;
}
function readDerElement(bytes, offset) {
  if (offset >= bytes.length) throw new Error("Invalid certificate.");
  const tag = bytes[offset]; let length = bytes[offset + 1]; let headerLength = 2;
  if (length === undefined) throw new Error("Invalid certificate length.");
  if (length & 0x80) { const lengthBytes = length & 0x7f; if (!lengthBytes || lengthBytes > 4 || offset + 2 + lengthBytes > bytes.length) throw new Error("Invalid certificate length."); length = 0; for (let index = 0; index < lengthBytes; index += 1) length = length * 256 + bytes[offset + 2 + index]; headerLength += lengthBytes; }
  const start = offset + headerLength; const end = start + length; if (end > bytes.length) throw new Error("Invalid certificate bounds."); return { tag, start, end, next: end };
}
function extractSubjectPublicKeyInfoFromCertificate(certificateBytes) {
  const certificate = readDerElement(certificateBytes, 0); if (certificate.tag !== 0x30) throw new Error("Invalid certificate.");
  const tbs = readDerElement(certificateBytes, certificate.start); if (tbs.tag !== 0x30) throw new Error("Invalid certificate TBS.");
  let offset = tbs.start; let element = readDerElement(certificateBytes, offset); if (element.tag === 0xa0) offset = element.next;
  for (let index = 0; index < 5; index += 1) { element = readDerElement(certificateBytes, offset); offset = element.next; }
  const subjectPublicKeyInfo = readDerElement(certificateBytes, offset); if (subjectPublicKeyInfo.tag !== 0x30) throw new Error("Invalid certificate public key.");
  return certificateBytes.slice(offset, subjectPublicKeyInfo.end);
}
export async function verifyFirebaseIdToken(request) {
  const authorization = request.headers.get("Authorization") || ""; if (!authorization.startsWith("Bearer ")) throw new Error("Missing bearer token.");
  const token = authorization.slice(7).trim(); const parts = token.split("."); if (parts.length !== 3) throw new Error("Invalid token format.");
  const [encodedHeader, encodedPayload, encodedSignature] = parts; const header = decodeBase64UrlJson(encodedHeader); const payload = decodeBase64UrlJson(encodedPayload);
  if (header.alg !== "RS256" || !header.kid) throw new Error("Invalid token header.");
  const now = Math.floor(Date.now() / 1000);
  if (!payload.sub || typeof payload.sub !== "string") throw new Error("Invalid token subject.");
  if (payload.aud !== FIREBASE_PROJECT_ID) throw new Error("Invalid token audience.");
  if (payload.iss !== FIREBASE_ISSUER) throw new Error("Invalid token issuer.");
  if (!Number.isFinite(payload.exp) || payload.exp <= now) throw new Error("Token expired.");
  if (!Number.isFinite(payload.iat) || payload.iat > now) throw new Error("Invalid token issue time.");
  if (!Number.isFinite(payload.auth_time) || payload.auth_time > now) throw new Error("Invalid authentication time.");
  const keys = await getFirebaseKeys(); let certificate = keys.get(header.kid);
  if (!certificate) { firebaseKeysExpiresAt = 0; await getFirebaseKeys(); certificate = firebaseKeyCache.get(header.kid); }
  if (!certificate) throw new Error("Unknown Firebase signing key.");
  const pem = certificate.replace(/-----BEGIN CERTIFICATE-----|-----END CERTIFICATE-----|\s/g, "");
  const der = Uint8Array.from(atob(pem), (character) => character.charCodeAt(0)); const spki = extractSubjectPublicKeyInfoFromCertificate(der);
  const key = await crypto.subtle.importKey("spki", spki, { name: "RSASSA-PKCS1-v1_5", hash: "SHA-256" }, false, ["verify"]);
  const valid = await crypto.subtle.verify("RSASSA-PKCS1-v1_5", key, base64UrlToBytes(encodedSignature), new TextEncoder().encode(`${encodedHeader}.${encodedPayload}`));
  if (!valid) throw new Error("Invalid Firebase token signature."); return payload;
}
async function requireAdminRole(token, env) {
  const admin = await env.DB.prepare("SELECT firebase_uid, role, is_enabled FROM admin_users WHERE firebase_uid = ? AND is_enabled = 1").bind(token.sub).first();
  if (!admin) throw new Error("Administrator authorization required.");
  return admin;
}
async function handleAdminAuthCheck(request, env) {
  try {
    const token = await verifyFirebaseIdToken(request);
    const admin = await requireAdminRole(token, env);
    return jsonResponse({ success: true, data: { authenticated: true, authorized: true, uid: token.sub, email: token.email || "", role: admin.role } });
  } catch (error) {
    if (error?.message === "Administrator authorization required.") return jsonResponse({ success: false, error: error.message }, 403);
    return jsonResponse({ success: false, error: "Authentication required." }, 401);
  }
}

async function getStoreSettings(env) {
  const settings = await env.DB.prepare(`SELECT id, store_name, logo_url, tagline, description, contact_email, contact_phone, whatsapp_url, address, settings_json, updated_at FROM store_settings WHERE id = 1`).first();
  const socialLinks = await env.DB.prepare(`SELECT id, platform, label, url, sort_order, is_enabled, created_at, updated_at FROM social_links WHERE is_enabled = 1 ORDER BY sort_order ASC, id ASC`).all();
  if (!settings) return { store: null, social_links: socialLinks.results ?? [] };
  let additionalSettings = {}; try { additionalSettings = JSON.parse(settings.settings_json || "{}"); } catch { additionalSettings = {}; }
  const { settings_json: _settingsJson, ...store } = settings;
  const emailSettings = getEmailSettings(settings);
  return { store: { ...store, additional_settings: { ...additionalSettings, email: emailSettings } }, social_links: socialLinks.results ?? [] };
}
async function handleStoreSettings(request, env) {
  if (request.method === "GET") { try { return jsonResponse({ success: true, data: await getStoreSettings(env) }); } catch { return jsonResponse({ success: false, error: "Unable to load store settings." }, 500); } }
  if (request.method !== "PUT") return jsonResponse({ success: false, error: "Method not allowed." }, 405);
  try {
    const token = await verifyFirebaseIdToken(request);
    await requireAdminRole(token, env);
    const body = await request.json();
    const allowedFields = ["store_name", "logo_url", "tagline", "description", "contact_email", "contact_phone", "whatsapp_url", "address"];
    const values = allowedFields.map((field) => String(body?.[field] ?? "").trim());
    let existingAdditionalSettings = {}; const currentSettings = await env.DB.prepare(`SELECT settings_json FROM store_settings WHERE id = 1`).first();
    try { existingAdditionalSettings = JSON.parse(currentSettings?.settings_json || "{}"); } catch { existingAdditionalSettings = {}; }
    const incomingEmail = body?.email_notifications || {};
    const emailSettings = {
      enabled: Boolean(incomingEmail.enabled),
      provider: String(incomingEmail.provider || "resend").trim().toLowerCase() || "resend",
      sender_name: String(incomingEmail.sender_name ?? "").trim(),
      sender_email: String(incomingEmail.sender_email ?? "").trim(),
      reply_to: String(incomingEmail.reply_to ?? "").trim(),
      notify_order_confirmation: Boolean(incomingEmail.notify_order_confirmation),
      notify_order_status: Boolean(incomingEmail.notify_order_status),
      notify_new_chat: Boolean(incomingEmail.notify_new_chat)
    };
    if (!["resend", "gmail"].includes(emailSettings.provider)) return jsonResponse({ success: false, error: "Unsupported email provider." }, 400);
    const additionalSettings = { ...existingAdditionalSettings, hero_headline: String(body?.hero_headline ?? "").trim(), email: emailSettings };
    await env.DB.prepare(`UPDATE store_settings SET store_name = ?, logo_url = ?, tagline = ?, description = ?, contact_email = ?, contact_phone = ?, whatsapp_url = ?, address = ?, settings_json = ?, updated_at = CURRENT_TIMESTAMP WHERE id = 1`).bind(...values, JSON.stringify(additionalSettings)).run();
    const socialLinks = Array.isArray(body?.social_links) ? body.social_links : []; const supportedPlatforms = new Set(["facebook", "instagram", "tiktok", "youtube", "whatsapp"]);
    for (const social of socialLinks) {
      if (!supportedPlatforms.has(String(social?.platform))) continue; const platform = String(social.platform); const label = String(social?.label ?? "").trim(); const url = String(social?.url ?? "").trim(); const sortOrder = Number.isFinite(Number(social?.sort_order)) ? Number(social.sort_order) : 0; const enabled = social?.is_enabled ? 1 : 0;
      const existing = await env.DB.prepare(`SELECT id FROM social_links WHERE platform = ? ORDER BY id ASC LIMIT 1`).bind(platform).first();
      if (existing?.id) await env.DB.prepare(`UPDATE social_links SET label = ?, url = ?, sort_order = ?, is_enabled = ?, updated_at = CURRENT_TIMESTAMP WHERE id = ?`).bind(label, url, sortOrder, enabled, existing.id).run();
      else await env.DB.prepare(`INSERT INTO social_links (platform, label, url, sort_order, is_enabled) VALUES (?, ?, ?, ?, ?)`).bind(platform, label, url, sortOrder, enabled).run();
    }
    return jsonResponse({ success: true, data: { uid: token.sub } });
  } catch (error) { if (error?.message === "Authentication required.") return jsonResponse({ success: false, error: error.message }, 401);
    if (error?.message === "Administrator authorization required.") return jsonResponse({ success: false, error: error.message }, 403);
    return jsonResponse({ success: false, error: "Unable to save store settings." }, 500); }
}
async function createCloudinarySignature(params, secret) {
  const entries = Object.entries(params).filter(([, value]) => value !== undefined && value !== null && value !== "").sort(([a], [b]) => a.localeCompare(b));
  const query = entries.map(([key, value]) => `${key}=${value}`).join("&");
  const digest = await crypto.subtle.digest("SHA-1", new TextEncoder().encode(`${query}${secret}`));
  return Array.from(new Uint8Array(digest), (byte) => byte.toString(16).padStart(2, "0")).join("");
}
async function handleImageUpload(request, env) {
  if (request.method !== "POST") return jsonResponse({ success: false, error: "Method not allowed." }, 405);
  try {
    const token = await verifyFirebaseIdToken(request);
    await requireAdminRole(token, env);
    if (!env.CLOUDINARY_CLOUD_NAME || !env.CLOUDINARY_API_KEY || !env.CLOUDINARY_API_SECRET) return jsonResponse({ success: false, error: "Cloudinary upload is not configured." }, 500);
    const contentLength = Number(request.headers.get("Content-Length") || 0); if (contentLength > 10 * 1024 * 1024) return jsonResponse({ success: false, error: "Image is too large. Maximum size is 10 MB." }, 413);
    const form = await request.formData(); const file = form.get("file");
    if (!(file instanceof File)) return jsonResponse({ success: false, error: "An image file is required." }, 400);
    if (!file.type.startsWith("image/")) return jsonResponse({ success: false, error: "Only image files are allowed." }, 400);
    if (file.size > 10 * 1024 * 1024) return jsonResponse({ success: false, error: "Image is too large. Maximum size is 10 MB." }, 413);
    const timestamp = Math.floor(Date.now() / 1000); const folder = "clothing-store/products"; const signature = await createCloudinarySignature({ folder, timestamp }, env.CLOUDINARY_API_SECRET);
    const cloudinaryForm = new FormData(); cloudinaryForm.append("file", file, file.name || "product-image"); cloudinaryForm.append("api_key", env.CLOUDINARY_API_KEY); cloudinaryForm.append("timestamp", String(timestamp)); cloudinaryForm.append("folder", folder); cloudinaryForm.append("signature", signature);
    const response = await fetch(`https://api.cloudinary.com/v1_1/${encodeURIComponent(env.CLOUDINARY_CLOUD_NAME)}/image/upload`, { method: "POST", body: cloudinaryForm });
    const result = await response.json().catch(() => ({}));
    if (!response.ok || !result.secure_url) return jsonResponse({ success: false, error: result.error?.message || "Cloudinary upload failed." }, 502);
    return jsonResponse({ success: true, data: { uid: token.sub, image_url: result.secure_url, cloudinary_public_id: result.public_id || "", original_filename: result.original_filename || file.name || "" } }, 201);
  } catch (error) { if (error?.message === "Authentication required.") return jsonResponse({ success: false, error: error.message }, 401); if (error?.message === "Administrator authorization required.") return jsonResponse({ success: false, error: error.message }, 403); return jsonResponse({ success: false, error: "Unable to upload image." }, 500); }
}
function normalizeSlug(value) { return String(value ?? "").trim().toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "").slice(0, 120); }
function parseOptionalId(value) { const id = Number(value); return Number.isInteger(id) && id > 0 ? id : null; }
function parseNonNegativeInteger(value, fallback = 0) { const number = Number(value); return Number.isInteger(number) && number >= 0 ? number : fallback; }
function parseNonNegativeNumber(value, fallback = 0) { const number = Number(value); return Number.isFinite(number) && number >= 0 ? number : fallback; }
function skuPrefix(value) { const words=String(value??"").toUpperCase().replace(/[^A-Z0-9]+/g," ").trim().split(/\s+/).filter(Boolean); if(!words.length)return "PROD"; if(words.length===1)return words[0].slice(0,6); return words.map(w=>w[0]).join("").slice(0,6); }
async function generateUniqueSku(env, seed) { const base=skuPrefix(seed); for(let n=1;n<10000;n++){const sku=base+"-"+String(n).padStart(3,"0"); const product=await env.DB.prepare(`SELECT id FROM products WHERE sku=? LIMIT 1`).bind(sku).first(); if(product)continue; const option=await env.DB.prepare(`SELECT id FROM product_group_options WHERE sku=? LIMIT 1`).bind(sku).first(); if(!option)return sku;} throw new Error("Unable to generate a unique SKU."); }
async function generateUniqueOptionSku(env, groupName, optionValue) { const group=skuPrefix(groupName); const option=skuPrefix(optionValue).slice(0,6); const base=group+"-"+option; for(let n=1;n<10000;n++){const sku=base+"-"+String(n).padStart(3,"0"); const product=await env.DB.prepare(`SELECT id FROM products WHERE sku=? LIMIT 1`).bind(sku).first(); if(product)continue; const existing=await env.DB.prepare(`SELECT id FROM product_group_options WHERE sku=? LIMIT 1`).bind(sku).first(); if(!existing)return sku;} throw new Error("Unable to generate a unique SKU."); }
function parseEnabled(value, fallback = 1) { if (value === undefined || value === null) return fallback; return value ? 1 : 0; }
function parseProductStatus(value, fallback = "draft") { const status = String(value ?? fallback).trim().toLowerCase(); return ["draft", "active", "archived"].includes(status) ? status : null; }
async function getOrCreateProductGroup(env,name){const n=String(name??"").trim();if(!n)return null;const slug=normalizeSlug(n);if(!slug)return null;const e=await env.DB.prepare(`SELECT id FROM product_groups WHERE slug=?`).bind(slug).first();if(e)return e.id;const x=await env.DB.prepare(`INSERT INTO product_groups (name,slug,relationship_type) VALUES (?,?,'color') RETURNING id`).bind(n,slug).first();return x?.id??null;}
async function getProductGroups(env){const result=await env.DB.prepare(`SELECT pg.id,pg.name,pg.slug,pg.relationship_type,pg.category_id,c.name AS category_name,c.parent_id AS category_parent_id,c.is_main_category AS category_is_main,pg.created_at,pg.updated_at FROM product_groups pg LEFT JOIN categories c ON c.id=pg.category_id ORDER BY pg.name COLLATE NOCASE ASC,pg.id ASC`).all();const groups=result.results??[];if(!groups.length)return groups;const ids=groups.map(g=>g.id),ph=ids.map(()=>"?").join(", ");const or=await env.DB.prepare(`SELECT id,product_group_id,value,sku,quantity,sort_order,created_at,updated_at FROM product_group_options WHERE product_group_id IN (`+ph+`) ORDER BY sort_order ASC,id ASC`).bind(...ids).all();const m=new Map();for(const o of or.results??[]){if(!m.has(o.product_group_id))m.set(o.product_group_id,[]);m.get(o.product_group_id).push(o);}for(const g of groups)g.options=m.get(g.id)??[];return groups;}
async function handleProductGroups(request,env){if(request.method==="GET"){try{const token=await verifyFirebaseIdToken(request);await requireAdminRole(token,env);return jsonResponse({success:true,data:await getProductGroups(env)});}catch(error){if(error?.message==="Administrator authorization required.")return jsonResponse({success:false,error:error.message},403);return jsonResponse({success:false,error:"Authentication required."},401);}}try{const token=await verifyFirebaseIdToken(request);await requireAdminRole(token,env);const body=await request.json();if(request.method==="POST"){const name=String(body?.name??"").trim(),relationshipType=String(body?.relationship_type??"color").trim().toLowerCase(),categoryId=body?.category_id===null||body?.category_id===""?null:parseOptionalId(body?.category_id),options=Array.isArray(body?.options)?body.options:[];if(!name)return jsonResponse({success:false,error:"Group name is required."},400);if(!["color","size"].includes(relationshipType))return jsonResponse({success:false,error:"Invalid grouping type."},400);if(!options.length)return jsonResponse({success:false,error:"At least one group option is required."},400);if(categoryId&&!(await env.DB.prepare(`SELECT id FROM categories WHERE id=?`).bind(categoryId).first()))return jsonResponse({success:false,error:"Category not found."},400);const slug=normalizeSlug(name);if(!slug)return jsonResponse({success:false,error:"Group name is invalid."},400);const group=await env.DB.prepare(`INSERT INTO product_groups(name,slug,relationship_type,category_id) VALUES(?,?,?,?) RETURNING id`).bind(name,slug,relationshipType,categoryId).first();for(let i=0;i<options.length;i++){const value=String(options[i]?.value??"").trim();if(!value)return jsonResponse({success:false,error:"Every group option needs a value."},400);const optionSku=String(options[i]?.sku??"").trim()||await generateUniqueOptionSku(env,name,value);await env.DB.prepare(`INSERT INTO product_group_options(product_group_id,value,sku,quantity,sort_order) VALUES(?,?,?,?,?)`).bind(group.id,value,optionSku,parseNonNegativeInteger(options[i]?.quantity),i).run();}return jsonResponse({success:true,data:{id:group.id},uid:token.sub},201);}if(request.method==="PUT"){const id=parseOptionalId(new URL(request.url).searchParams.get("id"));if(!id)return jsonResponse({success:false,error:"A valid group id is required."},400);if(!(await env.DB.prepare(`SELECT id FROM product_groups WHERE id=?`).bind(id).first()))return jsonResponse({success:false,error:"Product group not found."},404);const name=String(body?.name??"").trim(),relationshipType=String(body?.relationship_type??"color").trim().toLowerCase(),categoryId=body?.category_id===null||body?.category_id===""?null:parseOptionalId(body?.category_id),options=Array.isArray(body?.options)?body.options:null;if(!name||!["color","size"].includes(relationshipType))return jsonResponse({success:false,error:"Valid group name and grouping type are required."},400);if(categoryId&&!(await env.DB.prepare(`SELECT id FROM categories WHERE id=?`).bind(categoryId).first()))return jsonResponse({success:false,error:"Category not found."},400);await env.DB.prepare(`UPDATE product_groups SET name=?,slug=?,relationship_type=?,category_id=?,updated_at=CURRENT_TIMESTAMP WHERE id=?`).bind(name,normalizeSlug(name),relationshipType,categoryId,id).run();if(options){for(const o of options)if(!String(o?.value??"").trim())return jsonResponse({success:false,error:"Every group option needs a value."},400);const existingOptions=(await env.DB.prepare(`SELECT id,value FROM product_group_options WHERE product_group_id=? ORDER BY id ASC`).bind(id).all()).results??[];const existingByValue=new Map(existingOptions.map((o)=>[String(o.value).trim().toLowerCase(),o]));const incomingKeys=new Set();for(let i=0;i<options.length;i++){const o=options[i],value=String(o.value).trim(),key=value.toLowerCase();if(incomingKeys.has(key))return jsonResponse({success:false,error:"Group options must be unique."},400);incomingKeys.add(key);const existing=existingByValue.get(key);if(existing){await env.DB.prepare(`UPDATE product_group_options SET value=?,sku=?,quantity=?,sort_order=?,updated_at=CURRENT_TIMESTAMP WHERE id=?`).bind(value,String(o.sku??"").trim(),parseNonNegativeInteger(o.quantity),i,existing.id).run();}else{await env.DB.prepare(`INSERT INTO product_group_options(product_group_id,value,sku,quantity,sort_order) VALUES(?,?,?,?,?)`).bind(id,value,String(o.sku??"").trim(),parseNonNegativeInteger(o.quantity),i).run();}}for(const existing of existingOptions){const key=String(existing.value).trim().toLowerCase();if(incomingKeys.has(key))continue;const used=await env.DB.prepare(`SELECT COUNT(*) AS total FROM products WHERE product_group_option_id=?`).bind(existing.id).first();if(Number(used?.total||0)>0)return jsonResponse({success:false,error:"A group option assigned to a product cannot be removed. Reassign the product first."},409);await env.DB.prepare(`DELETE FROM product_group_options WHERE id=?`).bind(existing.id).run();}}return jsonResponse({success:true,data:{id,uid:token.sub}});}if(request.method==="DELETE"){const id=parseOptionalId(new URL(request.url).searchParams.get("id"));if(!id)return jsonResponse({success:false,error:"A valid group id is required."},400);if(!(await env.DB.prepare(`SELECT id FROM product_groups WHERE id=?`).bind(id).first()))return jsonResponse({success:false,error:"Product group not found."},404);await env.DB.prepare(`DELETE FROM products WHERE product_group_id=?`).bind(id).run();await env.DB.prepare(`DELETE FROM product_group_options WHERE product_group_id=?`).bind(id).run();await env.DB.prepare(`DELETE FROM product_groups WHERE id=?`).bind(id).run();return jsonResponse({success:true,data:{id,uid:token.sub}});}return jsonResponse({success:false,error:"Method not allowed."},405);}catch(error){if(error?.message==="Administrator authorization required.")return jsonResponse({success:false,error:error.message},403);if(String(error?.message||"").includes("UNIQUE constraint failed"))return jsonResponse({success:false,error:"A product group or option with that value already exists."},409);return jsonResponse({success:false,error:String(error?.message||"Unable to save product group.")},500);}}
async function getCategories(env,includeDisabled=false){
  const columns=`id,name,slug,description,parent_id,sort_order,is_enabled,is_main_category,show_in_navigation,show_on_homepage,is_featured,is_promoted,showcase_image_url,showcase_image_alt,created_at,updated_at`;
  const query=includeDisabled?`SELECT ${columns} FROM categories ORDER BY sort_order ASC,id ASC`:`SELECT ${columns} FROM categories WHERE is_enabled=1 ORDER BY sort_order ASC,id ASC`;
  const result=await env.DB.prepare(query).all();return result.results??[];
}
async function handleCategories(request,env){
  if(request.method==="GET"){try{const includeDisabled=new URL(request.url).searchParams.get("include_disabled")==="1";if(includeDisabled){try{await verifyFirebaseIdToken(request);}catch{return jsonResponse({success:false,error:"Authentication required."},401);}}return jsonResponse({success:true,data:await getCategories(env,includeDisabled)});}catch{return jsonResponse({success:false,error:"Unable to load categories."},500);}}
  try{
    const token=await verifyFirebaseIdToken(request);await requireAdminRole(token,env);if(!["POST","PUT","DELETE"].includes(request.method))return jsonResponse({success:false,error:"Method not allowed."},405);
    if(request.method==="POST"){const body=await request.json(),name=String(body?.name??"").trim(),slug=normalizeSlug(body?.slug||name);if(!name||!slug)return jsonResponse({success:false,error:"Category name is required."},400);const parentId=body?.parent_id===null||body?.parent_id===""?null:parseOptionalId(body?.parent_id);const main=body?.is_main_category?1:0;if(main&&parentId!==null)return jsonResponse({success:false,error:"Main categories cannot have a parent category."},400);const result=await env.DB.prepare(`INSERT INTO categories(name,slug,description,parent_id,sort_order,is_enabled,is_main_category,show_in_navigation,show_on_homepage,is_featured,is_promoted,showcase_image_url,showcase_image_alt) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?) RETURNING id`).bind(name,slug,String(body?.description??"").trim(),parentId,parseNonNegativeInteger(body?.sort_order),parseEnabled(body?.is_enabled),main,parseEnabled(body?.show_in_navigation,1),parseEnabled(body?.show_on_homepage),parseEnabled(body?.is_featured),parseEnabled(body?.is_promoted),String(body?.showcase_image_url??"").trim(),String(body?.showcase_image_alt??"").trim()).first();return jsonResponse({success:true,data:{id:result.id,uid:token.sub}},201);}
    const id=parseOptionalId(new URL(request.url).searchParams.get("id"));if(!id)return jsonResponse({success:false,error:"A valid category id is required."},400);
    if(request.method==="DELETE"){await env.DB.prepare(`DELETE FROM categories WHERE id=?`).bind(id).run();return jsonResponse({success:true,data:{id,uid:token.sub}});}
    const body=await request.json(),existing=await env.DB.prepare(`SELECT * FROM categories WHERE id=?`).bind(id).first();if(!existing)return jsonResponse({success:false,error:"Category not found."},404);
    const name=String(body?.name??existing.name).trim(),slug=normalizeSlug(body?.slug||name||existing.slug),parentId=body?.parent_id===null||body?.parent_id===""?null:parseOptionalId(body?.parent_id),main=body?.is_main_category===undefined?Number(existing.is_main_category||0):body.is_main_category?1:0;if(!name||!slug)return jsonResponse({success:false,error:"Category name is required."},400);if(main&&parentId!==null)return jsonResponse({success:false,error:"Main categories cannot have a parent category."},400);
    await env.DB.prepare(`UPDATE categories SET name=?,slug=?,description=?,parent_id=?,sort_order=?,is_enabled=?,is_main_category=?,show_in_navigation=?,show_on_homepage=?,is_featured=?,is_promoted=?,showcase_image_url=?,showcase_image_alt=?,updated_at=CURRENT_TIMESTAMP WHERE id=?`).bind(name,slug,String(body?.description??existing.description??"").trim(),parentId,parseNonNegativeInteger(body?.sort_order,existing.sort_order),parseEnabled(body?.is_enabled,existing.is_enabled),main,parseEnabled(body?.show_in_navigation,existing.show_in_navigation),parseEnabled(body?.show_on_homepage,existing.show_on_homepage),parseEnabled(body?.is_featured,existing.is_featured),parseEnabled(body?.is_promoted,existing.is_promoted),String(body?.showcase_image_url??existing.showcase_image_url??"").trim(),String(body?.showcase_image_alt??existing.showcase_image_alt??"").trim(),id).run();
    return jsonResponse({success:true,data:{id,uid:token.sub}});
  }catch(error){if(error?.message==="Authentication required.")return jsonResponse({success:false,error:error.message},401);if(error?.message==="Administrator authorization required.")return jsonResponse({success:false,error:error.message},403);if(String(error?.message||"").includes("UNIQUE constraint failed"))return jsonResponse({success:false,error:"A category with that slug already exists."},409);return jsonResponse({success:false,error:"Unable to save category."},500);}
}
async function getProducts(env,request){const p=new URL(request.url).searchParams,status=p.get("status"),productId=parseOptionalId(p.get("id")),categoryId=parseOptionalId(p.get("category_id")),groupId=parseOptionalId(p.get("product_group_id")),search=String(p.get("search")||"").trim(),page=Math.max(1,parseNonNegativeInteger(p.get("page"),1)),limit=Math.min(100,Math.max(1,parseNonNegativeInteger(p.get("limit"),24))),offset=(page-1)*limit,publicOnly=status===null;let sql=`SELECT p.id,p.category_id,p.product_group_id,p.product_group_option_id,p.product_group_value,p.name,p.slug,p.description,p.sku,p.price,p.currency,p.status,p.stock_quantity,p.track_stock,p.sort_order,p.created_at,p.updated_at,c.name AS category_name,c.slug AS category_slug,pg.name AS product_group_name,pg.slug AS product_group_slug,pg.relationship_type AS product_group_relationship_type,pgo.value AS product_group_option_value,pgo.sku AS product_group_option_sku,pgo.quantity AS product_group_option_quantity FROM products p LEFT JOIN categories c ON c.id=p.category_id LEFT JOIN product_groups pg ON pg.id=p.product_group_id LEFT JOIN product_group_options pgo ON pgo.id=p.product_group_option_id WHERE 1=1`;const b=[];if(publicOnly)sql+=` AND p.status='active'`;else if(!["draft","active","archived"].includes(status))return{error:"Invalid product status.",status:400};else{sql+=` AND p.status=?`;b.push(status);}if(productId){sql+=` AND p.id=?`;b.push(productId);}if(categoryId){sql+=` AND p.category_id=?`;b.push(categoryId);}if(groupId){sql+=` AND p.product_group_id=?`;b.push(groupId);}if(search){sql+=` AND (p.name LIKE ? OR p.sku LIKE ? OR p.description LIKE ?)`;const t="%"+search+"%";b.push(t,t,t);}const count=await env.DB.prepare(sql.replace(/SELECT[\s\S]*?FROM products p/,"SELECT COUNT(*) AS total FROM products p")).bind(...b).first();sql+=` ORDER BY p.sort_order ASC,p.id DESC LIMIT ? OFFSET ?`;b.push(limit,offset);const products=(await env.DB.prepare(sql).bind(...b).all()).results??[];if(products.length){const ids=products.map(x=>x.id),ph=ids.map(()=>"?").join(", ");const [imgs,variants]=await Promise.all([
      env.DB.prepare(`SELECT id,product_id,image_url,cloudinary_public_id,alt_text,sort_order,is_primary,created_at FROM product_images WHERE product_id IN (`+ph+`) ORDER BY sort_order ASC,id ASC`).bind(...ids).all(),
      env.DB.prepare(`SELECT id,product_id,name,sku,option_values,price_override,stock_quantity,is_enabled,sort_order,created_at,updated_at FROM product_variants WHERE product_id IN (`+ph+`) ORDER BY sort_order ASC,id ASC`).bind(...ids).all()
    ]).then(([a,b])=>[a.results??[],b.results??[]]);
    const ib=new Map(),vb=new Map();
    for(const x of imgs){if(!ib.has(x.product_id))ib.set(x.product_id,[]);ib.get(x.product_id).push(x);}
    for(const x of variants){let ov={};try{ov=JSON.parse(x.option_values||"{}")}catch{}x.option_values=ov;if(!vb.has(x.product_id))vb.set(x.product_id,[]);vb.get(x.product_id).push(x);}
    for(const x of products){x.images=ib.get(x.id)??[];x.variants=vb.get(x.id)??[];x.related_products=[];x.product_group_value=x.product_group_option_value||x.product_group_value||"";}if(publicOnly){const gids=[...new Set(products.map(x=>x.product_group_id).filter(Boolean))];if(gids.length){const gp=gids.map(()=>"?").join(", ");const related=(await env.DB.prepare(`SELECT p.id,p.product_group_id,p.product_group_option_id,p.product_group_value,p.name,p.price,p.currency,p.stock_quantity,p.track_stock,p.status,pgo.value AS product_group_option_value FROM products p LEFT JOIN product_group_options pgo ON pgo.id=p.product_group_option_id WHERE p.status='active' AND p.product_group_id IN (`+gp+`) ORDER BY p.sort_order ASC,p.id DESC`).bind(...gids).all()).results??[];const rg=new Map();for(const x of related){x.product_group_value=x.product_group_option_value||x.product_group_value||"";if(!rg.has(x.product_group_id))rg.set(x.product_group_id,[]);rg.get(x.product_group_id).push(x);}for(const x of products)x.related_products=(rg.get(x.product_group_id)??[]).filter(y=>y.id!==x.id);}}}return{data:{products,pagination:{page,limit,total:Number(count?.total||0),total_pages:Math.ceil(Number(count?.total||0)/limit)}}};}
async function handleProducts(request,env){if(request.method==="GET"){try{const r=await getProducts(env,request);if(r.error)return jsonResponse({success:false,error:r.error},r.status);return jsonResponse({success:true,data:r.data});}catch{return jsonResponse({success:false,error:"Unable to load products."},500);}}try{const token=await verifyFirebaseIdToken(request);await requireAdminRole(token,env);if(!["POST","PUT","DELETE"].includes(request.method))return jsonResponse({success:false,error:"Method not allowed."},405);const url=new URL(request.url),id=parseOptionalId(url.searchParams.get("id"));if(request.method==="DELETE"){if(!id)return jsonResponse({success:false,error:"A valid product id is required."},400);if(!(await env.DB.prepare(`SELECT id FROM products WHERE id=?`).bind(id).first()))return jsonResponse({success:false,error:"Product not found."},404);await env.DB.prepare(`DELETE FROM products WHERE id=?`).bind(id).run();return jsonResponse({success:true,data:{id,uid:token.sub}});}const body=await request.json();let e=null;if(request.method==="PUT"){if(!id)return jsonResponse({success:false,error:"A valid product id is required."},400);e=await env.DB.prepare(`SELECT id,category_id,product_group_id,product_group_option_id,product_group_value,name,slug,description,sku,price,currency,status,stock_quantity,track_stock,sort_order FROM products WHERE id=?`).bind(id).first();if(!e)return jsonResponse({success:false,error:"Product not found."},404);}const name=String(body?.name??e?.name??"").trim(),slug=normalizeSlug(body?.slug||name||e?.slug);if(!name||!slug)return jsonResponse({success:false,error:"Product name is required."},400);const st=parseProductStatus(body?.status,e?.status||"draft");if(!st)return jsonResponse({success:false,error:"Invalid product status."},400);const categoryId=body?.category_id===null||body?.category_id===""?null:parseOptionalId(body?.category_id??e?.category_id);if(categoryId&&!(await env.DB.prepare(`SELECT id FROM categories WHERE id=?`).bind(categoryId).first()))return jsonResponse({success:false,error:"Category not found."},400);let groupId=e?.product_group_id??null;if(Object.prototype.hasOwnProperty.call(body??{},"product_group_id"))groupId=body.product_group_id===null||body.product_group_id===""?null:parseOptionalId(body.product_group_id);let optionId=e?.product_group_option_id??null;if(Object.prototype.hasOwnProperty.call(body??{},"product_group_option_id"))optionId=body.product_group_option_id===null||body.product_group_option_id===""?null:parseOptionalId(body.product_group_option_id);let groupValue=String(body?.product_group_value??e?.product_group_value??"").trim();if(groupId){if(!(await env.DB.prepare(`SELECT id FROM product_groups WHERE id=?`).bind(groupId).first()))return jsonResponse({success:false,error:"Product group not found."},400);if(optionId){const o=await env.DB.prepare(`SELECT id,value FROM product_group_options WHERE id=? AND product_group_id=?`).bind(optionId,groupId).first();if(!o)return jsonResponse({success:false,error:"The selected group option does not belong to this group."},400);groupValue=o.value;}else if(!groupValue)return jsonResponse({success:false,error:"Select one of the predefined group options."},400);}else{optionId=null;groupValue="";}const vals=[categoryId,groupId,optionId,groupValue,"","",name,slug,String(body?.description??e?.description??"").trim(),String(body?.sku??e?.sku??"").trim()||await generateUniqueSku(env,name),parseNonNegativeNumber(body?.price,e?.price??0),String(body?.currency??e?.currency??"ZAR").trim().toUpperCase()||"ZAR",st,parseNonNegativeInteger(body?.stock_quantity,e?.stock_quantity??0),parseEnabled(body?.track_stock,e?.track_stock??1),parseNonNegativeInteger(body?.sort_order,e?.sort_order??0)];let productId=id;if(request.method==="POST"){const x=await env.DB.prepare(`INSERT INTO products(category_id,product_group_id,product_group_option_id,product_group_value,product_color,product_size,name,slug,description,sku,price,currency,status,stock_quantity,track_stock,sort_order) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?) RETURNING id`).bind(...vals).first();productId=x.id;}else await env.DB.prepare(`UPDATE products SET category_id=?,product_group_id=?,product_group_option_id=?,product_group_value=?,product_color=?,product_size=?,name=?,slug=?,description=?,sku=?,price=?,currency=?,status=?,stock_quantity=?,track_stock=?,sort_order=?,updated_at=CURRENT_TIMESTAMP WHERE id=?`).bind(...vals,productId).run();if(Array.isArray(body?.images)){await env.DB.prepare(`DELETE FROM product_images WHERE product_id=?`).bind(productId).run();for(const image of body.images){const u=String(image?.image_url??"").trim();if(!u)continue;await env.DB.prepare(`INSERT INTO product_images(product_id,image_url,cloudinary_public_id,alt_text,sort_order,is_primary) VALUES(?,?,?,?,?,?)`).bind(productId,u,String(image?.cloudinary_public_id??"").trim(),String(image?.alt_text??"").trim(),parseNonNegativeInteger(image?.sort_order),parseEnabled(image?.is_primary)).run();}}return jsonResponse({success:true,data:{id:productId,uid:token.sub}},request.method==="POST"?201:200);}catch(error){if(error?.message==="Authentication required.")return jsonResponse({success:false,error:error.message},401);if(String(error?.message||"").includes("UNIQUE constraint failed"))return jsonResponse({success:false,error:"A product with that slug already exists."},409);return jsonResponse({success:false,error:"Unable to save product."},500);}}
export default { async fetch(request, env) { const url = new URL(request.url); if (url.pathname === "/api/admin-auth-check") return handleAdminAuthCheck(request, env); if (url.pathname === "/api/store-settings") return handleStoreSettings(request, env); if (url.pathname === "/api/upload-image") return handleImageUpload(request, env); if (url.pathname === "/api/categories") return handleCategories(request, env); if (url.pathname === "/api/product-groups") return handleProductGroups(request, env); if (url.pathname === "/api/products") return handleProducts(request, env); return new Response(JSON.stringify({ success: true, message: "Clothing Store Worker is online.", version: "1.0.0-test" }), { status: 200, headers: { "Content-Type": "application/json" } }); } };